platform :ios, '13.0'

abstract_target 'CometChatTargets' do
  use_frameworks!

  pod 'CometChatCallsSDK'
  pod 'CometChatSDK'

  target 'CometChatUIKit' do
    # Specific settings for CometChatUIKit (if any)
  end

  target 'sample-app-push-notification' do
    # Specific settings for sample-app-push-notification (if any)
  end
end
